{% extends 'base.html.twig' %}

{% block body %}

    <h3 class="titleUnderline">{{ body }}</h3>

    <div class="row">
        <div class="col-sm-12 row">
            <div class="col-sm-3"><h1>Nom : </h1></div>
            <div class="col-sm-9"><h1>{{ current_User.lastname }}</h1></div>

        </div>
        <div class="col-sm-12 row">
            <div class="col-sm-3"><h1>Prénom : </h1></div>
            <div class="col-sm-9"><h1>{{ current_User.firstname }}</h1></div>
        </div>
        <div class="col-sm-12 row">
            <div class="col-sm-3"><h1>Email : </h1></div>
            <div class="col-sm-9"><h1>{{ current_User.email }}</h1></div>
        </div>
        <div class="col-sm-12 row">
            <div class="col-sm-3"><h1>Pseudo : </h1></div>
            <div class="col-sm-9"><h1>{{ current_User.pseudo }}</h1></div>
        </div>
    </div>

{% endblock %}